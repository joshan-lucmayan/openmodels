# Changelog

This directory tracks OpenSystem release changes.

See the top-level `CHANGELOG.md` for the canonical changelog. Per-release
notes may be kept here as `vX.Y.Z.md` files for detailed evolution records.
